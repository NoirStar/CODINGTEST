const solution = (operations) => {
    let answer = [];
    let queue = [];
    
    operations.map(operation => {
        if(operation[0] === 'D') {
            queue.push(operation.substring(2));
        } else {
            
        }
    });
    return answer;
};